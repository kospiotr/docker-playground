version: '2'
services:
    app:
        extends:
            file: app-prod.yml
            service: app
    db:
        extends:
            file: app-prod.yml
            service: db
        image: "hypriot/rpi-mysql"
